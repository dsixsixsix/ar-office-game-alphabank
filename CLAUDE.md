# Office Game (Alfa-Bank concept)

Студенческий проект. Мобильная 2D-игра, мотивирующая сотрудников приходить в офис: задания выполняются физически в офисе, за них начисляется внутриигровая валюта, которую можно обменять на награды (в том числе «реальные» из каталога). Игра нигде не публикуется.

Визуальный стиль вдохновлён Hotline Miami 2: Wrong Number: детализированный пиксель-арт, неон, резкий контраст, CRT/VHS-эффекты, тряска камеры. Игра вертикальная (портретная ориентация телефона). Квартира игрока и офис показаны в виде 3/4 сверху (стены и мебель спереди, видны лица и одежда персонажей), катсцены интро — вид сбоку в комикс-панелях. Пропорции персонажей и предметов реалистичные. Все ассеты (спрайты, музыка, шрифты, персонажи) — собственные или под свободной лицензией; ассеты оригинальной игры не используются.

## Версии

- **v1 — мобильная (основная):** Android и iOS, нативные сборки из Godot.
- **v2 — веб (запасная):** Web-экспорт того же проекта Godot. Делается, если с мобильными сборками возникнут сложности или понадобится демо без установки.

Обе версии собираются из одной кодовой базы клиента. Платформенный код изолирован за интерфейсами (см. «Платформенный слой»).

## Стек

### Клиент

| Область | Выбор |
|---|---|
| Движок | Godot 4.x (последний stable) |
| Язык | GDScript со строгой статической типизацией (не C#: C# не экспортируется в Web) |
| Рендерер | Compatibility (OpenGL ES 3 / WebGL 2) — одинаковая картинка на мобильных и в вебе, максимальный охват устройств |
| Разрешение | Базовое 360×640 (портрет), stretch mode `viewport`, aspect `expand`, целочисленное масштабирование, фильтр текстур `Nearest`, snap 2D-трансформаций к пикселям |
| Уровни | Godot `TileMapLayer`, тайл 32 px, персонажи 32×48 px в кадре 48×64 с местом под аксессуары (4 направления), y-сортировка мебели и персонажей. Спрайт игрока рисуется в рантайме из сохранённого образа (гардероб) |
| Анимация | `AnimationPlayer` / `AnimatedSprite2D`, импорт из Aseprite (плагин Aseprite Wizard) |
| Эффекты | `canvas_item`-шейдеры: полноэкранный пост-процесс на `CanvasLayer` (CRT, scanlines, хроматическая аберрация, VHS-шум, виньетка), glow через `WorldEnvironment`, `GPUParticles2D`/`CPUParticles2D`, тряска камеры через `Camera2D` |
| UI | Godot Control-ноды, пиксельный шрифт, темы `.tres` |
| Аудио | Audio buses Godot (музыка, SFX, UI) |
| Локализация | Встроенная (`tr()`, CSV/PO), основной язык — русский |
| Сеть | Официальный клиент `nakama-godot` (HTTP + WebSocket) |
| Тесты | GdUnit4 |
| Линт/формат | gdtoolkit (`gdlint`, `gdformat`) |

### Платформенный слой

Функции, которых нет в ядре Godot, закрыты интерфейсом `PlatformServices` (автозагрузка) с реализациями под каждую платформу:

| Функция | Android / iOS (v1) | Web (v2) |
|---|---|---|
| Сканирование QR | Нативный плагин: Android — Godot Android Plugin v2 + ML Kit; iOS — GDExtension-плагин + AVFoundation | `JavaScriptBridge` + `getUserMedia` + jsQR |
| Геолокация | Нативный плагин: Android — Fused Location Provider; iOS — CoreLocation | `JavaScriptBridge` + Geolocation API |
| BLE-маяки (опционально) | Нативный плагин | Не поддерживается |
| Уведомления (опционально) | Нативный плагин (FCM / APNs) | Не поддерживается |

Игровой код обращается только к `PlatformServices`, никогда напрямую к плагинам или `JavaScriptBridge`.

### Сервер

| Область | Выбор |
|---|---|
| Игровой сервер | Nakama (open-source, Apache 2.0): аутентификация, аккаунты, кошелёк с леджером, хранилище, лидерборды, RPC |
| Серверная логика | Go runtime-модуль Nakama (плагин, собирается образом `heroiclabs/nakama-pluginbuilder` той же версии, что и сервер) |
| БД | PostgreSQL |
| Аутентификация | Email + пароль Nakama (для демо — также device auth) |
| Админка | React + TypeScript + Vite, клиент `nakama-js`; вызывает админские RPC (каталог наград, задания, модерация обменов) |
| Локальный запуск | Docker Compose (Nakama + PostgreSQL) |
| Тесты | `go test` |
| Линт | `golangci-lint` |

### Инструменты

- Aseprite — спрайты и анимации.
- Git + GitHub Actions — CI: линт и тесты клиента/сервера, экспорт сборок (образ `barichello/godot-ci`).

## Архитектура

```
[Godot-клиент: Android / iOS / Web]
        │  HTTPS / WebSocket (nakama-godot)
        ▼
[Nakama + Go-модуль] ── PostgreSQL
        ▲
        │  HTTPS (nakama-js)
[Админка React]
```

## Обязательные правила

1. **Сервер — единственный источник истины.** Клиент никогда не начисляет валюту, не засчитывает задания и не выдаёт награды. Клиент отправляет действие через RPC, сервер проверяет и меняет состояние.
2. **Экономика только через кошелёк Nakama** (`WalletUpdate` с метаданными операции). Каждое начисление и списание пишется в леджер. Все RPC, меняющие баланс, идемпотентны (ключ операции от клиента, проверка на сервере).
3. **Проверка присутствия в офисе — минимум два фактора, проверяются на сервере:**
   - ротируемый QR-код на экране в офисе (TOTP-подобный токен, срок жизни 30–60 с, секрет только на сервере);
   - геозона офиса по координатам (слабый фактор, только в паре с QR);
   - опционально — BLE-маяк зоны.
4. **Антифрод на сервере:** лимиты начислений в день, запрет повторного использования QR-токена, журнал подозрительных действий, ручное подтверждение обмена на дорогие награды.
5. **Секреты** (ключи Nakama, TOTP-секреты, пароли БД) не коммитятся. Только через переменные окружения и `.env` в `.gitignore`.
6. **Платформенные API** — только через `PlatformServices` (см. выше), чтобы v2 (Web) не требовала переписывать игровой код.

## Планируемая структура репозитория

```
client/        Godot-проект
  addons/      плагины Godot (nakama, GdUnit4, Aseprite Wizard)
  platform/    PlatformServices и реализации под платформы
  scenes/
  scripts/
  shaders/
  assets/
  tests/
server/        Go-модуль Nakama
  docker-compose.yml
admin/         React-админка
docs/
```

## Соглашения

- GDScript: статические типы везде, `snake_case` для функций и переменных, `PascalCase` для классов и нод, один скрипт — одна ответственность, сигналы вместо прямых ссылок между несвязанными сценами.
- Go: стандартный `gofmt`, ошибки не игнорируются, каждое RPC валидирует входные данные.
- Коммиты: Conventional Commits (`feat:`, `fix:`, `docs:`, `refactor:`, `test:`, `chore:`).
- Основная ветка: `main`.
