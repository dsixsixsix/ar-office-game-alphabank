// Versions follow the Godot 4.7.2 Android build template (config.gradle) so the plugin and the
// exported app use the same toolchain.
plugins {
    id("com.android.library") version "8.6.1" apply false
    id("org.jetbrains.kotlin.android") version "2.1.21" apply false
}
